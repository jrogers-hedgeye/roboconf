#!/usr/bin/env bash

source "$(dirname "$0")/roboconf.sh"
ROB_CONF_DEBUG=true
banner "Roboconf banner test"
banner "This is a longer banner text to test how the centering works when the text length exceeds the target width of the banner display."
